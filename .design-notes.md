# 设计笔记

## 视觉方向（frontend-design 第三轮，2026-09-24 · 定稿）

**主题语言：Apple 产品页式简约（refined minimalism）**

用户否决了本轮早先的"水文档案"方案（坐标纸 + 水尺 + 印章 + 印泥红，评价"太丑"），
明确要求 Apple 风格、简约高级、突出重点。档案方案的全部元素（GaugeRail 水尺、
Seal 印章、楷体、Anton、JetBrains Mono、坐标纸底纹）已删除，git 历史可回捞。

- **调色**：apple.com 同款灰阶 —— 页面 #FFF、卡片 #FBFBFD、主文字 #1D1D1F、
  次要 #86868B、发丝线 #D2D2D7、唯一强调色 Apple 蓝 #0071E3；
  状态色用系统绿 #30D158 / 琥珀 #FF9F0A（成果状态点）。暗色 = 纯黑 + #1C1C1E 卡片 + #2997FF。
- **字体**：Inter Variable（SF Pro 最接近的自托管方案，latin 子集）+ 系统黑体。
  展示与正文同一字体，层级只靠字号/字重/灰度 —— 这是"突出重点"的主要手段。
- **首屏即全部简介**：Hero 左栏（头像/名字/简介/CTA/实绩行）+ 右栏四条能力卡（2×2），
  「关于」板块已删除、原文并入 Hero 简介句。用户要求：进来一屏就完成初步了解。
  用户明确不要 Hero 卡片/签名图（FloodChart、hydro.ts、Seal、GaugeRail 全删）。
- **组件语言**：18px 圆角卡片（.card：极淡描边 + 柔和投影，暗 mode 换白描边）、
  hover 浮起 3px（.card-hover）、全圆角 pill 按钮与 chip、设置页式分组列表（技能/成果）、
  frosted glass 页头（backdrop-blur + 白色 70% 透明）。
- **动效纪律**：全站只做一种入场 —— fadeUp（opacity + 14px 位移，0.6s，60ms 级联），
  lib/motion.ts 统一出口。滚动进度 = 页头下缘一根 2px 蓝线（useSpring）。
- **卡片纪律（2026-09-24 用户反馈"全是白卡太多"后确立）**：
  卡片只留给"主打"内容（Hero 四能力、项目/开源的 featured、教育、成果），
  经历改为发丝线列表行、项目/开源网格改为发丝线行；板块用白/灰全宽色带交替（App 里包 band）。
  白带上的技能表反转为灰底分组列表 + 白 chip；Hero 能力卡图标各配 iOS 系统色（蓝/靛/绿/橙）。

## 技术要点

- token 名称（paper/sheet/ink-*/line/accent）全保留，PrintResume 与导出 PDF 零改动。
- `.card` / `.chip` 是 index.css @layer 外的普通类（暗色用 @media + .dark-root 覆写阴影）。
- 数字对齐用 `tnum`（tabular-nums），不再引等宽字体。
- `prefers-reduced-motion` 全局关动画；framer MotionConfig reducedMotion="user"。
- 视口高于文档时 useScroll 进度线渲染异常（scaleX 满宽）——真实屏幕不会出现，已知不修。

## 折叠区教训（2026-08-09 第二轮后用户反馈）

用户："进去之后看不到主要内容"。测量：第一屏 900px 高，About 起点 949px —— **遥测卡 350px 把全部简历内容顶到首屏之下**。

修复：Hero 改桌面两栏 `grid-cols-[minmax(0,1fr)_380px]`，左栏文字 / 右栏紧凑遥测卡（过程线随卡宽自动 ~90px）。结果：Hero 551px，About 起点上移到 608px，进入首屏。

**教训：签名元素再好看也不能吃满首屏 —— 首屏必须露出现实内容（简历），签名靠边站。** 大屏上签名元素适合"侧栏小部件"，不适合"整条横幅"。

## 教训（2026-09-24 第三轮）

**"有性格"不等于"好看" —— 用户要的是高级感，不是叙事感。** 档案方案的每个元素都"有出处"，
但叠在一起是怀旧，不是高级。Apple 风格的要点是克制：一个签名元素、一种强调色、
一种入场动效，其余全部让位给内容的层级。

## 待做 / 候选

- 移动端导航（当前 sm 以下隐藏 nav 链接，只有"联系"）。
- 头像 `public/avatar.jpg` 尚未提供。
- GitHub Actions 自动部署到 GitHub Pages 未配。
