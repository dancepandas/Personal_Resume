# 设计笔记

## 视觉方向（frontend-design 第二轮，2026-08-09）

**主题语言：测站遥测仪表（hydro / discharge-station）**

从用户领域（洪水预报、水文监测、PINN 圣维南方程）提取的意象，取代了第一轮的"通用极简"。

- **签名元素**：Hero 的遥测仪表卡 —— 一条洪水过程线（lognormal 型：起涨→峰→退水），滚动时绘制动画，一个 "now" 指示点沿曲线巡游，卡片下方一行等宽遥测读数 `repos=07 stars=02 patents=03`。
- **水位进度条**：页头滚动进度即"水位上涨"（`scaleX` + useSpring）。
- **区块标题**：装饰性 `01/02/03` 序号 → 测站铭牌（迷你过程线 HydroMark + 等宽英文标签 `ABOUT` / `EXPERIENCE` …）。
- **配色**：纸面 `#FBFCFD`、墨色 slate 系（`#0F172A` 最深）、发丝线 `#E3E8EF`、单点"水文蓝" `#0E7490`（不再用泛用 `#2563eb`）。
- **字体**：正文系统无衬线，所有"数据/日期/仓库名/标签"用等宽（JetBrains Mono）——像仪表读数。
- **一个真冒险的决定**：Hero 从"大名字+标语"改成"测站仪表"，其余保持安静、克制。

## 技术要点

- `hydro.ts`：Catmull-Rom → Bezier 平滑路径；lognormal `μ=0.72 σ=0.5`，采样 `n=120`（主图）/ `320`（指示点插值）。
- HydroMark 与主图共用 `1000×240` 坐标，靠 viewBox 缩放（不要对小尺寸单独生成——坐标会越界）。
- now 指示点用 `useMotionValue + animate` 驱动解析点插值，比 CSS `offset-path` 兼容性稳。
- `prefers-reduced-motion` 全局关动画；`:focus-visible` 用 accent。

## 折叠区教训（2026-08-09 第二轮后用户反馈）

用户："进去之后看不到主要内容"。测量：第一屏 900px 高，About 起点 949px —— **遥测卡 350px 把全部简历内容顶到首屏之下**。

修复：Hero 改桌面两栏 `grid-cols-[minmax(0,1fr)_380px]`，左栏文字 / 右栏紧凑遥测卡（过程线随卡宽自动 ~90px）。结果：Hero 551px，About 起点上移到 608px，进入首屏。

**教训：签名元素再好看也不能吃满首屏 —— 首屏必须露出现实内容（简历），签名靠边站。** 大屏上签名元素适合"侧栏小部件"，不适合"整条横幅"。

## 待做 / 候选

- 移动端导航（当前 sm 以下隐藏 nav 链接，只有"联系"）。
- 头像 `public/avatar.jpg` 尚未提供。
- GitHub Actions 自动部署到 GitHub Pages 未配。
